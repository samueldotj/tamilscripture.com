// Shared state of the page chrome on phones (design 10A): the header and the
// reader's thumb bar slide away together while reading and come back together.
export const chrome = $state({ hidden: false });
