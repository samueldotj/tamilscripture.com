{
  "name": "tamilscripture",
  "private": true,
  "description": "Tamil Bible Online - www.tamilscripture.com",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "content": "node scripts/fetch-stepbible.mjs && cargo run --release -p usfm-ingest -- --books data/books.toml --xrefs data/xrefs/cross_references.txt --audio data/audio --out apps/web/static/content data/versions && cargo run --release -p entity-ingest -- --books data/books.toml --entities data/entities --content apps/web/static/content",
    "content:names": "node scripts/fetch-stepbible.mjs && cargo run --release -p entity-ingest -- --books data/books.toml --entities data/entities --content apps/web/static/content --draft-names",
    "content:fixture": "node scripts/fetch-stepbible.mjs && cargo run -p usfm-ingest -- --books data/books.toml --xrefs data/xrefs/cross_references.txt --audio data/fixtures/audio --out apps/web/static/content data/fixtures/versions && cargo run -p entity-ingest -- --books data/books.toml --entities data/entities --content apps/web/static/content",
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "check": "pnpm --filter web check",
    "test": "cargo test --workspace && pnpm --filter web test"
  },
  "devDependencies": {
    "supabase": "^2.116.0"
  }
}
